{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["build/**", "build/client/**"]
    },
    "typecheck": {
      "dependsOn": ["^build"]
    },
    "lint:fix": {}
  }
}
